enum FormStatus {
  intial,
  inProgress,
  submittingLoading,
  succesSubmission,
  failedSubmission,
}

enum ProfilePageStatus { initial, loading, loaded, editting }

enum HomePageStatus { home, search, addPost, favorites, settings }

enum PostsViewStatus { initial, loading, loaded, fail }
